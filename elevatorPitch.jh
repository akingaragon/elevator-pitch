entity ElevatorPitch {
	title String,
    description String,
    videoUrl String,
    thumbnailUrl String,
    likeNumber Integer,
    liked Boolean
}

relationship ManyToOne {
    ElevatorPitch{inventor} to User
}

// Set pagination options
paginate ElevatorPitch with infinite-scroll

service all with serviceImpl

dto * with mapstruct
